@model CourseProject.Models.LessonType

@{
    ViewBag.Title = "Видалити";
}

<h2>Видалити</h2>

<h3>Ви впевнені, що хочете видалити запис?</h3>
<fieldset>
    <legend>LessonType</legend>

    <div class="display-label">
         <h3><label>ID типу заняття</label></h3>
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Lesson_ID)
    </div>

    <div class="display-label">
         <h3><label>Тип</label></h3>
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Type)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Видалити" /> |
        @Html.ActionLink("Назад до списку", "Index")
    </p>
}
