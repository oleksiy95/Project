@model IEnumerable<CourseProject.Models.LessonType>

@{
    ViewBag.Title = "Тип заняття";
}

<h2>Тип заняття</h2>

<p>
    @Html.ActionLink("Додати запис", "Create")
</p>
<table>
    <tr>
       
        <th>
            <label class="labelForSchedule">Тип заняття</label>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
       
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
       <td>
            @Html.ActionLink("Редагувати", "Edit", new { id=item.Lesson_ID }) |
            @Html.ActionLink("Деталі", "Details", new { id=item.Lesson_ID }) |
            @Html.ActionLink("Видалити", "Delete", new { id=item.Lesson_ID })
        </td>
    </tr>
}

</table>
